package de.dkfz.roddy.config;

/**
 * Configuration constants are values which can be set within configuration xml files.
 * They can be marked in code for automatic lookup of available values.
 */
public @interface ConfigurationConstant {
}
